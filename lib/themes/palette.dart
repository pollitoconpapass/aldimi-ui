import 'package:flutter/material.dart';

const Color primaryBlue = Color(0xFF557CCE); // buttons, primary actions
const Color warmCoral = Color(0xFFED3232); // links, accents
const Color creamBackground = Color(0xFFF5F3EF);

// Neutrals
const Color deepTeal = Color(0xFF2D3436);
const Color softGray = Color(0xFFDFE6E9);
const Color paleLavender = Color(0xFFF0EEEB);
const Color white = Color(0xFFFFFFFF);
